import React from 'react';
import logo from '../img/LogoNormal.png'

const Logo = () => (
        <header>
            <img className="logo" src={logo} alt='logo' />
        </header>
)
    
export default Logo;